singularity {
  enabled = true
  autoMounts = true
  cacheDir = "/mnt/slow_storage/containers"
}

executor {
    name = "slurm"
    queue = "short"
    cpus = 60
    perCpuMemAllocation = true // 8Gb x CPUs per process
    
}